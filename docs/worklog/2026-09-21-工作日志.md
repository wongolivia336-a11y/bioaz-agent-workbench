# 2026-09-21 · 空间首页第三版：层级重排

用户看着预览提了两条：进了空间还挂着空间选择器，多余；页面信息层级不好，可以加 icon / 插画。

## 判断

- **选择器该去掉。** 面包屑写着空间名，选择器和它上面那行「你想在 X 中完成什么任务？」把同一件事说了三遍。进了空间，空间就是定的；要换去侧栏。选择器只留给全局首页。
- **层级问题的根子是七张卡一样重。** 四张专家卡 + 三张概览卡同一副边框、同一档字号，看不出谁是动作、谁是说明。
  顶上那个大 logo 是全局首页的品牌时刻，放在空间首页上主角就成了 BioAZ 而不是这个空间。

## 改了什么

| 位置 | 之前 | 现在 |
|---|---|---|
| 顶部 | BioAZ logo | 一张细线插画 `SpaceMark`（文件夹 + 文件 / 任务 / 专家三个节点，虚线连回，一处品牌紫）+ 一枚标签「客户空间 · 3 个任务」 |
| 专家卡 | 无组名 | 上方一行眉题「本空间的专家」 |
| 输入框上方 | 「你想在 X 中完成什么任务？」+ 空间选择器 | 都没了，只剩输入框 |
| 概览 | 三张跟专家卡同款的小卡 | 一条浅底横条，三组数字细线隔开，15px 数字、灰图标、无箭头，hover 才亮 |

`isSpaceHome` 一个判据管全部分支；全局首页一个像素没变（logo、选择器、提示语照旧）。
文件：`components/hero/SpaceMark.tsx`（新）、`NewTaskHome.tsx`、`WorkbenchShell.tsx`（传 `spaceKind`）、`globals.css`。

## 踩到的

- `.newTaskIntro` 的 `justify-items` 在某处被改成了 `start`，logo 和标题是各自写了 `justify-self: center` 才居中的。
  插画和标签第一次渲染贴在左边——也得各自声明，不能指望父级。用 `getBoundingClientRect` 量中心点确认三者都在 854。

## 验收（1440 宽）

进 XX 空间：插画 + 「客户空间 · 3 个任务」居中，h1 一行，眉题「本空间的专家」，无选择器无提示语，概览横条与输入框同宽同轴（424–1284）。

---

# 同日第四版：专家可配、卡有底纹、hover 介绍、概览成卡、紫色收一收

用户看了第三版又提了四条：这一页能不能直接配专家；专家卡里放一张任务角色的底纹插画；紫色太多太油、扫光柔一点；
概览也再推敲，hover 上去要有一张「灵动的」介绍卡，卡片同样要有 svg。中途一句「首屏不要改」，我已经把卡片的紫收成了全局的，
用户看了首屏之后说「现在这样可以不要撤回」——所以紫色那三处是全局的，其余全在 `isSpaceHome` 之下。

## 改了什么

| 位置 | 做法 |
|---|---|
| 眉题行 | 「本空间的专家」左，「配置专家」右，开的是侧栏 … 菜单那张同一个弹窗。`SpaceCoworkerDialog` 从 `WorkspaceSidebar.tsx` 抽到自己的文件，两处 import。宽度公式跟卡片网格同一个（几张卡 × 215px），三张卡时也对得齐 |
| 专家卡底纹 | `components/hero/RoleMotif.tsx`：按 moduleId 一张线稿（试管+价签 / 报告页+趋势线 / 细胞簇+价签 / 盾+勾），绝对定位右下角，`currentColor` = `--review-line`，hover 深一档仍是灰 |
| hover 介绍卡 | 放在 button 里的 `span.expertIntro`（button 里只能放短语内容），空间首页的卡 `overflow: visible`。内容全从模块定义上取：`suggestedCoworker.description` + `stages` 标签 + `artifacts` 标签，壳层拼进 `QuickStartItem.intro`。开在卡**下方**盖住输入框——先做的上方，盖住大标题黑字被截一半，比盖住空输入框难看得多。进场 `cubic-bezier(.2,.9,.3,1.15)` 一点回弹，流程 chip 按 `--i` 依次亮 |
| 概览 | 三张矮卡 `.spaceOverviewCard`：20px 数字 + 标签 + 一行说明，右侧 `OverviewMotif`（一叠纸 / 一页带印的产物 / 三行清单），箭头 hover 才滑进右上角 |
| 紫色 | 图标方块静态 `--review-body` 灰字、hover 才 `--review-brand`；卡边扫光峰值 `.82 → .26`；hover 边框 `#c6c2ed → --review-line-strong` |

## 踩到的

- **输入框那一栏 z-index 20**（它的菜单要压过介绍区）。介绍卡开在它上面，`.newTaskIntro` 只有 z-index 1 → 被压在下面。
  用 `.newTaskIntro.isSpaceHome:has(.taskExampleGrid button:hover) { z-index: 21 }` 只在 hover 时抬，常态仍让输入框的菜单压过来。
- `pointer-events: none` 的元素 `elementFromPoint` 会穿过去——验 z-order 时得临时把它打开再测，不然永远"被压住"。
- 浏览器面板在后台时 rAF 停摆，transition 永远停在起点：验 hover 态要临时 `transition: none` 再读 computed。
- 图标方块的底色其实是 `new-task-home.css` 里后加载的 `color-mix(5% 品牌色)` 定的，review.css 写什么都不生效——把 review.css 里那句删了、留注释指过去，不留一句骗人的 CSS。
- `audit:ui` 把 `120ms / 160ms / 220ms` 算成三种新时长——这几个文件里以前从来没写过字面量，全是 `var(--bioaz-motion-*)`。改回令牌，只剩 selectors +38 和 keyframe +1（`expertIntroChipIn`）是有意的，`--update`。
- dev server 的 watcher 漏了几次改动（G: 盘 + webpack cache 报 snapshot 失败），浏览器一直是旧包。停了重开就好。

## 验收（1440 宽）

眉题行与网格同宽（860 / 三张卡时 645）；四张卡各有底纹；hover DMPK 卡：介绍卡 272 宽开在卡下 10px、压在输入框之上、
四个流程 chip、产出「Word 报价单 · Excel 报价明细」；「配置专家」去掉 QA → 三张卡，侧栏 … 菜单里的「设置空间专家」看到同一份勾选；
概览三张卡 78 高、与输入框同轴。typecheck 过，audit baseline 已更新。

---

# 同日补丁：介绍卡加宽、流程改步骤条、弹窗被顶栏压住

用户看了截图：介绍卡太窄，文字分列难看；点「配置专家」之后整页糊了、唯独顶栏是清的。

## 介绍卡

272 → 400 宽。流程那一行不再是一串带箭头的 chip（4 步折两行、6 步折三行，标签列被挤得东一块西一块），
改成一条小步骤条：N 步 `grid-template-columns: repeat(var(--n), 1fr)` 等分整行，点排在一条线上（`::before` 从第一个点心到最后一个点心），
名字在点下面，最后一步实心。最多 6 步、名字最长 5 个字，等分后每格 ≥ 56px，11px 字永远不折行。
四张卡的介绍卡现在一样高（124px），4 / 5 / 6 步都一行。

## 弹窗

不是空间首页的问题，是 `<Dialog>` 一直就地渲染：遮罩 `position: fixed; z-index: 80`，但空间首页 `.newTaskHome` 是 `isolation: isolate`，
这个 80 只在容器内部算数，顶栏（容器外、有自己的 z-index）就压在遮罩上面。`PreviewModal` 早就 portal 到 body 了（注释里还写着
"三处走同一条路，才不会出现有一个弹窗被下面那层盖住"），`Dialog` 没跟上。现在跟上：`createPortal(..., document.body)`，
`typeof document` 守一下 SSR。7 个 `<Dialog>` 调用点全部受益，行为（Esc、点遮罩、层栈）不变——验过。

## 验收

介绍卡 400 宽、最后一张右对齐到卡；6 步的肿瘤报告一行放下；遮罩的父节点是 body，顶栏和侧栏中点 `elementFromPoint` 都打到遮罩；
Esc 和点遮罩都关。

---

# 同日补丁二：站内信复核画布里，计算表的明细压到参数表底下

用户截图：报价复核 → 计算表，左边的计价明细和右边的可编辑参数叠在一起。

## 原因

复核画布里那张纸只有 644 宽（右边还有人工批注栏），明细栏分到 307px。auto 布局的表格宽 = 各列最小宽度之和，
只要这个和超过 307，表格就从栏里溢出去、画在参数表底下——溢出不裁。三样东西把最小宽度撑到了 374：

1. 单价下面那行小注（P0-2 加的 `.quoteNumNote`）写了 `nowrap`，「In life -TOX- (per animal/week)」一行就是 200px。**主因，是我这轮加的。**
2. 条目名 `<strong>` 和说明 `<small>` 是两个 inline 元素挨着，「Species and strain」+「C57」连成一个词 "strainC57"——最小宽度按这个词算。这条是原来就有的（`.quoteCellButton` 那套样式定义了但 markup 没用上）。
3. 分类名 `nowrap`，「Clinical Pathology」不折就是 130px。

## 改法

不动表格布局（auto 照旧，宽屏下和原来一样），只把最小宽度压下去：小注允许折行、上限 128px 靠右；条目名和说明各占一行；
计算表里的分类名允许折行。改完明细表 307 = 栏宽，不再溢出；顺手把 "strainC57" 那种连字修了。

## 验收

1440 宽进 站内信 → 待确认：Balb/c nude 报价交付包 → 开始审核 → 计算表：明细表宽 307.45 = 栏宽，`overlap: false`，
参数表也没溢出纸边。

---

# 同日补丁三：弹窗里的「保存」变紫了

上一个补丁把 `Dialog` portal 到了 body，副作用：主按钮改黑那条规则是 `.dmpkShell .primaryButton` 这样按壳层限定的，
弹窗出了壳层就退回 iteration.css 的 `--primary`（#2900ff）。同一个原因，`.dmpkShell.workbenchShell .primaryButton.compact / .secondaryButton.compact { height: 36px }`
也够不着了，「取消」44 高、「保存」34 高——正是 Dialog.tsx 注释里说过的那对数字。

- review.css 「主按钮改黑」的名单加一行 `.modalBackdrop .primaryButton`（hover 同），portal 出去的弹窗都在这层底下。
- design-system.css 弹窗底部按钮那条改成 `.bioazUiDialog .bioazUiDialogFooter > button`，特异性压过两类名的尺寸规则，
  「两个按钮共享同一组尺寸」这句话才真的由它保证，不再靠壳层顺手拉平。

验过两处：空间首页「配置专家」和复核画布「通过」确认——都是 body 直属、`#17191e`、两个按钮 36 高。

---

# 下午：报价开发组会议 → 右栏「报价板块」

心蕊在上课没参会，回放要点由另一个 agent 从妙记抽出（飞书妙记要登录，两个浏览器都进不去）。

## 会上定的

1. **右栏重新定位**：不再收集 / 编辑参数，只做「已收集参数的状态展示 + 对应单价」；单价可改，改价是**临时调价的唯一路径**（陈曦慧总结，吴迪 / 开发同意）。
2. **完整价目表不进对话**：独立入口，有权限的人（SD 级）进去看全部底价表。
3. **参数不开放编辑，权限分级后置**：只有价格能改；「SD 助理改临时价、SD 改底表」这套权限本轮不含。

点名交付：① 右栏概览版（按板块：PK 样品采集机制、TOX、BA、ADA…，每板块只放待测物 / 检测方法 / 数量 / 单价，
描述性细节归成数量，不罗列来源 / 规则）② 临时改价闭环（告知原价 → 输入新价 → 确认）③ 完整价目表独立入口页（先出框架）
④ 概览确认后再细化各板块字段（参考吴迪的「八维」表）。另：「报价二 bug」是他们系统的待办。

用户拍板：A→B→C 先做，D 等概览确认；价目表入口用账号切换器做演示级「审批人 / 负责人才看得见」；动物、报告作为小板块排最后。

## 做了什么（A + B + C）

- `QuotePackage` 加 `ba`，顺序改成 pk-tk / tox / ba / ada / animal / report，标签按会上的叫法；`QuoteLine` 加 `analyte` / `method`。
- 新面板 **报价板块**：按板块分段，一行一项服务，行内 2×2 四要素（标签在上值在下——右栏三百多像素，四列并排放不下）。
  账一有行就切过去（一次性效果 + 一句话识别后的 suggestPanel）；传文件那条路仍停在参数收集，文件认出来的要人逐项确认。
- **行从十四项推**：以前只有读过方案才有账，现在没方案也有一张粗账（采血 / 检测 / 方法开发 / 动物 / 饲养 / 报告），
  缺哪项参数对应行就是「缺参数 → 去填这一项」。退回会话不要账（纸面是批注锚着的固定件）。
- **改价闭环**：原价 + 价目版本 → 新价 → 确认（失焦不落）；落下挂「临时价 · 原 ¥X」+ 恢复；数字同事落一句；谁改记谁的账号名。
  无价目行叫「补价」，chip 是「临时价 · 本单补价」。
- **价目表的门**：板块面板底部「查看完整价目表 →」进报价管理标准价格；撰写人看到的是一行说明。标准价格页从会话进来时页首多一行全局 / 本单的边界说明。
- 文档 `docs/QUOTE_SECTIONS.md`。

## 验收（1440，王林彬 / 赵敏两个账号）

- 王林彬新建 DMPK 会话，一句「PK 小分子，SD 大鼠，每组 6 只，3 组，2 周…」→ 右栏切到报价板块：PK / TK 3 项缺参数、动物使用 2 项 ¥5,940、报告缺参数；
  补分析方法后方法开发 ¥6,000 亮起；「SD 大鼠使用费」改价 120 → 100：原价行、确认、chip、对话一句、小计 5,940 → 5,580。
- 传 BB-001 方案：5 个板块 27 行 ¥723,280；「细胞因子检测」补价 200 → 临时价 · 本单补价，TOX 小计涨到 ¥144,410。
- 赵敏（撰写人）BA Only 会话：BA 板块 + 报告，没有动物板块；底部是「完整价目表只对审批人 / 负责人开放」那行字。
- 王林彬点「查看完整价目表」→ 报价管理标准价格，11 项，页首边界说明。
- 种子会话 Balb/c nude BA：BA 板块 48 份 × ¥180。

## 踩到的

- `resolveInspectorPanels` 的 `isDefault` 只在当前 tab 消失时兜底，真正的默认 tab 是会话里 `suggestPanel` 推的——所以「账一有行就切过去」得在会话里做，不是在注册表里。
- 一句话收集路径每轮都 `suggestPanel("parameters")`，会把板块 tab 抢回去；改成「推出来的账有行就 sections，没行才 parameters」。
- 会议例子里一个板块就是一行；我们一个板块里有多项服务。行首保留服务名——概览确认时要对颗粒度。

## 晚上补：板块折叠 + 精简，报价规则让位

用户看了第一版：太多太杂。改法：

- **板块可折叠**，段头写名字 / 几项 / 几项待定 / 小计；默认只开第一个有待定的板块。人点过的记住，没点过的按默认——新长出来的板块也能自己冒出来。
- **行压成两行**：名字 + 「128 份 × ¥130」，下一行「血清 · 6 个分析物 · LC-MS/MS」。2×2 带标签的表撤了。改价收成单价旁一支小铅笔（行 hover 才深），编辑器在行下展开；临时价写在行的第三行「临时价 · 原 ¥120 · 恢复」。
  BB-001 27 行：面板高度 1469 → 983（三个板块开）→ 一屏放下（只开一个）。
- **报价规则**只留规则：区域、模板 / 管理费两条 + 「单价不在这儿改」一句；无价目只报个数和类别（「免疫分型全血采血 · 细胞因子检测 · 流式检测」，四个组的采血算一类）；全局四项收成一行胶囊。
  撤掉的：动物费 / 报告费两条「规则」（是单价）、费用明细弹窗、「对话编辑」按钮——都是板块在做的事。
- 顺手删了 `dmpk-strategy.css`、`dmpk-adjustment-drawer.css`：两份文件里没有一个类名被任何 tsx 引用（旧的策略弹窗遗留），审计的死类名 −11。

## 明天

- 拿吴迪的「八维」表，定各板块字段（交付 ④）。
- Composer 侧积木卡（基础卡 + 板块卡），板块定义和右栏共用。
- 概览发给陈曦慧、吴迪确认。
